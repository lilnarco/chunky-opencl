# Layered-fog parity

Layered fog ports the CPU's logistic-CDF extinction over the path's y-span (whole-path span from the path origin, not reset by bounces), including sky fog out to the fog limit, using the same fully-sunlit inscatter approximation as the uniform fog. Layers are exported per render as triplets with a count.
